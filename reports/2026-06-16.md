# Daily Report — 2026-06-16

## 작업 요약

### 1. 당일주도주 연속 등장 추적 기능 추가 (`cf71ded`)
- `compute_consecutive_days(market, data)` 함수 추가 — 최근 5거래일 캐시를 순회해 동일 종목 연속 등장 횟수 계산
- `consecutive_days` 필드를 주도주 데이터에 추가, `leaders_tab` 테이블에 연속일 뱃지 표시 (🔥 3일이상 / ⚡ 2일)
- `append_to_daily_report()` 에 연속 등장 하이라이트 블록 추가

### 2. 일별 리포트 자동 생성 기능 추가 (`b29cc81`)
- `utils/report_generator.py` 신규: `generate_market_section()`, `append_to_daily_report()`
- `scripts/fetch_leaders_daily.py` 신규: 매일 11:00 / 15:30 주도주 수집 + 리포트 업데이트
- Windows Task Scheduler 등록 (11:00 / 15:30 KST)
- 출력: `quantReports/YYYY-MM-DD.html`

### 3. 포트폴리오 손절/목표가 알림 기능 추가 (`18b1220`)
- `holding_stop_loss`, `holding_target_price` State 변수 추가
- 보유종목 분석 탭에 손절선/목표가 카드 표시
- VWAP × 0.96 손절선 자동 계산

### 4. 섹터 모멘텀 뷰 추가 (`5c28fd8`, `2a0b4ce`)
- `utils/sector_scanner.py` 신규: KODEX ETF(KR) / SPDR ETF(US) 섹터 모멘텀 1M/3M/6M/12M
- 별도 탭 "섹터모멘텀" 분리

### 5. 일별 리포트 HTML 형식으로 변경 + 저장 경로 변경 (`e12578d`)
- 기존 Markdown → HTML 형식 전환
- 저장 경로: `reports/` → `quantReports/`

### 6. 섹터 모멘텀 확장 (`b7d86af`, `13adbf6`, `9c9f5ad`)
- 1M/3M/6M/12M 전체 기간 한 화면 표시
- KR 섹터에서 코드 오류·레버리지/인버스 ETF 제거
- `set_momentum_bt_years` setter 명시 추가 (Reflex 0.8.9 경고 해소)

### 7. 버그 수정
- `rx.foreach` 내 `consecutive_days >= 3` 비교 TypeError 수정 (`42d5d82`) — bool 플래그 미리 계산
- `test_tab_integration` 3건 실패 수정 (`a0df16a`)
- 종가매매 후보 없을 때 안내 메시지 표시 (`ef10ce2`)

---

## 커밋 목록

| 커밋 | 내용 |
|------|------|
| `cf71ded` | feat: 당일주도주 연속 등장 추적 기능 추가 |
| `b29cc81` | feat: 일별 리포트 자동 생성 기능 추가 |
| `18b1220` | feat: 포트폴리오 손절/목표가 알림 기능 추가 |
| `5c28fd8` | feat: 섹터 모멘텀 뷰 추가 |
| `a0df16a` | fix: test_tab_integration 3건 실패 수정 |
| `42d5d82` | fix: rx.foreach 내 consecutive_days >= 3 비교 TypeError 수정 |
| `ef10ce2` | feat: 종가매매 후보 없을 때 안내 메시지 표시 |
| `2a0b4ce` | feat: 섹터 모멘텀을 별도 탭으로 분리 |
| `e12578d` | feat: 일별 리포트를 HTML 형식으로 변경 + 저장 경로 변경 |
| `b7d86af` | feat: 섹터 모멘텀 1M/3M/6M/12M 전체 기간 한 화면 표시 |
| `13adbf6` | fix: KR 섹터 목록에서 코드 오류·레버리지/인버스 ETF 제거 |
| `9c9f5ad` | fix: set_momentum_bt_years setter 명시 추가 (Reflex 0.8.9 경고 해소) |
