# Daily Report — 2026-06-19

## 작업 요약

### 1. 기간 모멘텀 3개월 슬라이딩 윈도우 캐시 구현 (`b6238ac`)
- `utils/stock_scanner.py` 내 `save_momentum_cache_all()` / `load_momentum_cache_all()` 추가
- 캐시 파일: `cache/momentum_{MARKET}_{YYYYMMDD}.json`
- 3개월 슬라이딩 윈도우: 최근 90일치 캐시를 누적 보관, 30일 이상 된 파일 자동 정리
- `scan_stock_momentum_all_periods()` 신규 — 1W/1M/2M/3M 4개 기간 동시 계산, flat list 반환

### 2. 기간 모멘텀 전용 탭 분리 (`f303124`)
- `pmom_tab()` 함수 신규 분리
- 메인 탭 목록에 "기간모멘텀" 탭 추가
- State 변수: `pmom_results`, `pmom_market`, `pmom_period`, `pmom_from_cache`, `pmom_progress`

### 3. 기간모멘텀 4기간 통합 뷰 + 정렬 컬럼 이동 + 일별 자동수집 (`9d351c3`)
- 테이블에 1W/1M/2M/3M 4개 컬럼 동시 표시
- 정렬 기준 버튼 클릭 시 해당 컬럼이 col1(굵게·파란색)으로 이동, 나머지 3개 순서 유지
- `apply_sort_and_cols(rows, sort_period, top_n)` 함수 — Python 서버에서 col1~col4 위치 필드 미리 계산
- `scripts/fetch_momentum_daily.py` 신규 — 매일 16:00 KST 자동 수집 스크립트

---

## 커밋 목록

| 커밋 | 내용 |
|------|------|
| `b6238ac` | feat: 기간 모멘텀 3개월 슬라이딩 윈도우 캐시 구현 |
| `f303124` | feat: 기간 모멘텀 전용 탭 분리 (기간모멘텀 탭 추가) |
| `9d351c3` | feat: 기간모멘텀 탭 4기간 통합 뷰 + 정렬 시 컬럼 이동 + 일별 자동수집 |
