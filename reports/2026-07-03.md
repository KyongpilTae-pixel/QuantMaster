# 2026-07-03 개발 이력

## 작업 요약

### 1. 단위 테스트 현황 확인
- 전체 단위 테스트 300 passed (non-integration)
- 이전 세션 284 → 계절성 테스트 16건 추가로 300건

---

### 2. 추세추종 3단계 — 계절성 전략 탭 구현

**신규 파일**: `utils/seasonality.py`
- `calc_monthly_seasonality(df, entry_type, ma_period, hold_days, halflife_years)` 함수
- 진입 신호(pullback / breakout_n / box_breakout)별 1월~12월 EV 분석
- halflife 3년 지수감쇠 가중치 적용
- 반환: 12개 dict (month_num/month_kr/ev/win_rate/avg_profit/avg_loss/pl_ratio/sample_n + bool 플래그)

**State 추가** (`main/main.py`):
- `season_code`, `season_entry_type`, `season_ma_period`, `season_entry_label`
- `season_hold_days: int = 20`, `season_rows: List[dict]`, `season_loading: bool`

**이벤트 핸들러 추가**:
- `load_seasonality()`: asyncio.to_thread로 OHLCV 로드 → calc_monthly_seasonality 호출
- `set_season_hold_days()`: 보유기간 변경 시 자동 재계산 (5/10/20/60일)

**UI 추가**:
- `_season_panel()`: violet 테마, 보유기간 선택 버튼 4개, 12개월 × EV/승률/평균이익/손실/손익비/표본 테이블
- `trend_scanner_table()`: [계절성] 버튼(indigo) 추가, `_season_panel()` 연결

**테스트**: `tests/test_seasonality.py` 16건 신규 (모양/필드/bool플래그/진입유형/보유기간/포맷)

---

### 3. 당일주도주 Best Pick 카드

**기능**: 주도주 데이터 로드 시 일반주(is_etf=False) 중 A점수(score_a) 최고 종목 자동 선정

**State 추가**:
- `leaders_best_pick: List[dict] = []` (len 0 or 1 패턴 — Reflex rx.cond 안정성)

**메서드 추가**:
- `_compute_best_pick()`: try-except 방어, 추천이유 텍스트 생성 (당일 등락률/거래량순위/상승률순위/연속상승/신고가근접/A점수)
- `_apply_filter_and_sort()` 내에서 자동 호출

**UI 추가**:
- 순위 테이블 제일 상단에 amber 테마 카드 표시
- `rx.foreach(State.leaders_best_pick, ...)` 패턴으로 dict 필드 안전 접근
- [분석] 버튼으로 종목 조회 탭 바로 이동

**버그 수정** (별도 commit):
- 초기 구현 `rx.cond(State.leaders_best_name != "", ...)` 방식 → 카드 미표시
- List[dict] + `.length() > 0` 패턴으로 교체 (Reflex에서 가장 안정적인 조건 패턴)
- try-except 추가, 명시적 `rx.fragment()` false 브랜치

---

### 4. 섹터 모멘텀 기간 선택 정렬

**요구사항**: 기간 버튼 클릭 시 해당 기간 기준으로 섹터 순위 재정렬

**State 추가**:
- `sector_sort_period: str = "1m"` (기본값 1개월 유지)

**메서드 추가**:
- `_apply_sector_sort()`: sector_sort_period 기준 재정렬 + rank 갱신
- `set_sector_sort_period()`: 기간 변경 → 즉시 재정렬 (네트워크 재조회 없음)

**`sector_scanner.py` 변경**:
- 내부 1M 고정 정렬 제거 → State에서 담당

**UI 변경** (`sector_tab()`):
- 정렬 기준 버튼 5개 추가 (5일/1개월/3개월/6개월/12개월) — stock_momentum 탭과 동일 스타일
- `_sector_col_header()`: 활성 컬럼 파란색 + ▼ 표시, 헤더 클릭으로도 정렬 변경 가능

---

## 커밋 목록

| 해시 | 내용 |
|------|------|
| `945c1e5` | feat: 추세추종 계절성 전략 탭 + 당일주도주 Best Pick 카드 |
| `9d36876` | fix: Best Pick 카드 List[dict] 패턴으로 안정화 |
| `e361db1` | feat: 섹터 모멘텀 기간 선택 정렬 (5일/1개월/3개월/6개월/12개월) |

## 테스트 현황

| 구분 | 건수 | 상태 |
|------|------|------|
| 단위 테스트 (non-integration) | 300 | ✅ 전체 통과 |
| 통합 테스트 (test_tab_integration.py) | ~28 | 네트워크 필요 (별도 실행) |
